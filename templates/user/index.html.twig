{% extends 'base.html.twig' %} {% block title %} Hello RecipesController! {% endblock %} {% block body %}

    <div class="container mt-5 js-filter">
        <h1 class="text-center text-white">Profil</h1>

        {% if clear %}
            <span id="clear_local"></span>
        {% endif %}
{#        {{ dump(user) }}#}
        <div class="row mb-5">
            <div class="col-md-4 border bg-white ">
                <h2 class="border text-center w-100">à propos de moi</h2>
                <div class="d-flex justify-content-center">
                <span class="text-white bg-dark btn rounded-circle btn-xl">
                    JL
                </span>
                </div>
                <h3 class="text-center">{{ app.user.firstname |capitalize }} {{ app.user.lastname |upper }}</h3>
                <h4>Adresse</h4>
                <hr>
                <span class="ms-5">{{ app.user.address1 }} {{ app.user.address2 }}, {{ app.user.zipCode }} {{ app.user.idCity.name ?? '' }}</span>
                <h4>Réalisation</h4>
                <hr>
                <span>Nombre de commande éffectuer : {{ user.orders|length }} </span>
            </div>
            <div class="col-md-8">

                {# <ul class="nav nav-pills mb-3" id="pills-tab" role="tablist"> #}
                {# <li class="nav-item" role="presentation"> #}
                {# <button class="nav-link text-white active" id="pills-profile-tab" data-bs-toggle="pill" #}
                {# data-bs-target="#pills-profile " type="button" role="tab" aria-controls="pills-profile" #}
                {# aria-selected="true"> Profile #}
                {# </button> #}
                {# </li> #}
                {# <li class="nav-item" role="presentation"> #}
                {# <button class="nav-link text-white" id="pills-orders-tab" data-bs-toggle="pill" #}
                {# data-bs-target="#pills-orders" type="button" role="tab" aria-controls="pills-home" #}
                {# aria-selected="false">Commande #}
                {# </button> #}
                {# </li> #}
                {# </ul> #}
                {# <div class="tab-content" id="pills-tabContent"> #}

                {# #}{# {% include "user/_form.html.twig" %} #}
                {# #}{# {% include "user/_orders.html.twig" %} #}
                {# <div class="tab-pane pb-5 fade bg-white d-flex show active justify-content-center" #}
                {# id="pills-profile" role="tabpanel" aria-labelledby="pills-profile-tab"> #}
                {# #}{# {{ form(form) }} #}
                {# </div> #}
                {# <div class="tab-pane fade bg-white " id="pills-orders" role="tabpanel" #}
                {# aria-labelledby="pills-orders-tab"> #}
                {# order #}
                {# </div> #}
                {# </div> #}

                <ul class="nav nav-pills mb-3" id="myTab0" role="tablist">
                    <li class="nav-item" role="presentation">
                        <button
                                class="nav-link active  text-white"
                                id="home-tab0"
                                data-bs-toggle="tab"
                                data-bs-target="#home0"
                                type="button"
                                role="tab"
                                aria-controls="home"
                                aria-selected="true"
                        >
                            Profile
                        </button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button
                                class="nav-link  text-white"
                                id="profile-tab0"
                                data-bs-toggle="tab"
                                data-bs-target="#profile0"
                                type="button"
                                role="tab"
                                aria-controls="profile"
                                aria-selected="false"
                        >
                            Commandes
                        </button>
                    </li>
                </ul>
                <div class="tab-content text-white" id="myTabContent0 text-white">
                    <div
                            class="tab-pane fade show active bg-white "
                            id="home0"
                            role="tabpanel"
                            aria-labelledby="home-tab0"
                    >
                        <div class="d-flex  justify-content-center">

                              {% include "user/_form.html.twig" %}

                        </div>
                    </div>
                    <div class="tab-pane fade" id="profile0" role="tabpanel" aria-labelledby="profile-tab0">
{#                        {% include "user/_orders.html.twig" %}#}
                    </div>
                </div>

            </div>


        </div>
    </div {% endblock %}